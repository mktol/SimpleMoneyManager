package com.mtol.checker.repository;

import com.mtol.checker.entity.ExpectedSpending;
import org.springframework.data.repository.CrudRepository;

/**
 * This class
 */
public interface ExpectedSpendingRepository extends CrudRepository<ExpectedSpending, Long> {
}
